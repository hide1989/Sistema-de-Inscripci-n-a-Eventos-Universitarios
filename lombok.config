# Lombok configuration for this project
# Instruct Lombok to copy @Qualifier annotations from fields
# to the constructor parameters it generates via @RequiredArgsConstructor.
# Without this, Spring cannot disambiguate beans when multiple implementations
# of the same port interface exist (imperative vs functional).
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier
